import styled from "styled-components";

export const CategoryFilter = styled.div`
  display: flex;
  gap: 1rem;
  flex-wrap: wrap;
  font-size: 0.85rem;

  label {
    display: flex;
    gap: 0.25rem;
    align-items: center;
    cursor: pointer;
  }
`;
