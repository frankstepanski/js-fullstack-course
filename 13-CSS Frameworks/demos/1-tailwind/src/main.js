// main.js
//
// This is the JavaScript entry point for the project.
// Vite starts here when you run `npm run dev`.
//
// The most important line is the CSS import below.
// Even though this is a JavaScript file, Vite understands
// CSS imports and handles them automatically.
//
// Without this import, Tailwind's classes would not work —
// the browser would have no stylesheet to read from.

import './style.css'

//
// That's all the JS this project needs for now.
// As you add interactivity later (dropdowns, modals, etc.)
// you would write that JavaScript below this comment.
//
console.log('Tailwind profile page loaded via Vite!')
