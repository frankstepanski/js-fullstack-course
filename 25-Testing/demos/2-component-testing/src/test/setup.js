// src/test/setup.js
//
// This file runs once before all tests.
//
// It imports @testing-library/jest-dom which adds extra matchers
// to expect() — things like:
//
//   toBeInTheDocument()   — checks an element is on the page
//   toBeVisible()         — checks an element is visible
//   toHaveTextContent()   — checks an element's text
//   toBeDisabled()        — checks a form element is disabled
//
// Without this import, those matchers will not be available
// and you will see errors like "toBeInTheDocument is not a function".

import "@testing-library/jest-dom";
