# Don't copy these into the Docker image

# Dependencies are installed fresh inside the container
node_modules

# No need for git history inside the image
.git

# Never bake secrets into an image
.env
